{
  "name": "@kawaz/colorize",
  "version": "0.2.1",
  "description": "A powerful log colorization tool built with Chevrotain parser for beautiful and readable log output",
  "type": "module",
  "main": "./dist/colorize.js",
  "bin": {
    "colorize": "./bin/colorize"
  },
  "files": [
    "bin/",
    "dist/",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "dev": "bun run bin/colorize",
    "test": "bun test",
    "lint": "bunx biome check . --write",
    "build": "bun build src/cli.ts --outfile=dist/colorize.js --target=node --minify",
    "prepublishOnly": "bun run lint && bun run test && bun run build",
    "release": "bun run release:patch",
    "release:patch": "bun run prepublishOnly && npm version patch && git push && git push --tags",
    "release:minor": "bun run prepublishOnly && npm version minor && git push && git push --tags",
    "release:major": "bun run prepublishOnly && npm version major && git push && git push --tags"
  },
  "keywords": [
    "log",
    "logs",
    "colorize",
    "colorizer",
    "highlighting",
    "syntax",
    "parser",
    "chevrotain",
    "cli",
    "terminal",
    "console"
  ],
  "author": "kawaz",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kawaz/colorize.git"
  },
  "bugs": {
    "url": "https://github.com/kawaz/colorize/issues"
  },
  "homepage": "https://github.com/kawaz/colorize#readme",
  "engines": {
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "chevrotain": "^11.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.2",
    "@types/bun": "latest",
    "bun-types": "latest"
  }
}
